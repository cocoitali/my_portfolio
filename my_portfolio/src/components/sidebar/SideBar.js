import React, { Component } from 'react';
import {
    SideBarWrapper,
  } from "../Styled";

class SideBar extends Component {
    render() {
      return (
        <SideBarWrapper>
        </SideBarWrapper>
      );
    }
  }

export default SideBar;